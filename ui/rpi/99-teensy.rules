# Teensy (USB VID 0x16C0): tell ModemManager not to probe it as a modem, and
# give access to the dialout group so the UI can open /dev/ttyACM* without sudo.
SUBSYSTEM=="tty", ATTRS{idVendor}=="16c0", ENV{ID_MM_DEVICE_IGNORE}="1", GROUP="dialout", MODE="0660"
